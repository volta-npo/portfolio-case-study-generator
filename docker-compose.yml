services:
  app:
    build: .
    image: volta-oss/portfolio-case-study-generator:local
    ports:
      - "4173:8080"
    env_file:
      - .env.example
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
